"use strict";
//'use client' -> nextJS에서 사용하는 방법

const idDom = document.getElementById("id");
const idSelector = document.querySelector("#id");
console.log(idDom, idSelector);

// 외부에서 자바스크립트를 불러오는 방법
